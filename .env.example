# Arbiter Wallet (either PRIVATE_KEY or MNEMONIC required)
# In EigenCloud TEE, MNEMONIC is auto-generated by KMS at runtime
PRIVATE_KEY=0x...
# MNEMONIC=test test test test test test test test test test test junk

# EigenAI Configuration (grant-based auth via wallet signature)
EIGENAI_GRANT_SERVER=https://determinal-api.eigenarcade.com
EIGENAI_MODEL=gpt-oss-120b-f16

# Chain Configuration (Base Mainnet)
CHAIN_ID=8453
# RPC_URL= (uses viem default for Base Mainnet, or set custom)

# Operator (deployed via `pnpm seed`)
OPERATOR_ADDRESS=0x...
# OPERATOR_FEE_BPS=0 (0 = no fees; set before deploying operator)

# Arbiter Configuration
CONFIDENCE_THRESHOLD=0.7

# Default receiver for /api/disputes listing (optional)
# DEFAULT_RECEIVER=0x...

# Server port (default: 3000)
# PORT=3000

# --- Facilitator Server (pnpm facilitator) ---
# Uses PRIVATE_KEY, CHAIN_ID, RPC_URL from above
# Facilitator server port (default: 4022)
# FACILITATOR_PORT=4022

# --- Merchant Server (pnpm merchant) ---
# Merchant wallet (falls back to PRIVATE_KEY if not set)
# MERCHANT_PRIVATE_KEY=0x...
# Facilitator URL for payment verification
# FACILITATOR_URL=http://localhost:4022
# Merchant server port (default: 4021)
# MERCHANT_PORT=4021
