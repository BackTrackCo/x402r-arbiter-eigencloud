# x402r CLI Configuration
# These are defaults — override with ~/.x402r/config.json or env vars

# Required: Your private key (payer wallet)
PRIVATE_KEY=

# Operator contract address (deployed PaymentOperator)
OPERATOR_ADDRESS=

# Network (default: Base Sepolia)
NETWORK_ID=eip155:84532
RPC_URL=https://sepolia.base.org

# Arbiter server URL (default: localhost)
ARBITER_URL=http://localhost:3000

# Optional: Pinata API key for IPFS pinning
PINATA_API_KEY=
PINATA_SECRET_KEY=
